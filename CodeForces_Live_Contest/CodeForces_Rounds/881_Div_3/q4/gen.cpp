#include <bits/stdc++.h>

using namespace std;

    
    signed main(signed argc, char* argv[]){
        ios_base::sync_with_stdio(false);
        cin.tie(NULL);
        srand(atoi(argv[1]));
        int t = 5 ;
        while(t--){
            int n = rand() % 10 + 1 ;
            int m = rand() % 10 + 1 ;
            for(int i = 0 ; i < n ; ++i){
                int l = rand() % 10 + 1 ;
                int r = rand() % 10 + 1 ;
            }
            for(int i = 0 ; i < m ; ++i){
                int l = rand() % 10 + 1 ;
                int r = rand() % 10 + 1 ;
            }
            
        }
    
    return 0;
}